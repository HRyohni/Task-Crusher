---
phase: 16-push-notifications
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/AndroidManifest.xml
  - app/src/main/java/hr/fipu/organizationtool/ZenStackApplication.kt
  - app/src/main/java/hr/fipu/organizationtool/ui/TaskViewModel.kt
  - app/src/main/java/hr/fipu/organizationtool/MainActivity.kt
  - app/src/main/java/hr/fipu/organizationtool/ui/ZenStackApp.kt
autonomous: true

must_haves:
  truths:
    - "On first app launch (Android 13+), the user is shown the system POST_NOTIFICATIONS permission dialog"
    - "When an achievement is unlocked, a system notification appears in the notification shade with the achievement name and description"
    - "Tapping the notification opens the app with the Achievements tab selected"
  artifacts:
    - path: "app/src/main/AndroidManifest.xml"
      provides: "POST_NOTIFICATIONS permission declaration and singleTop launch mode"
      contains: "POST_NOTIFICATIONS"
    - path: "app/src/main/java/hr/fipu/organizationtool/ZenStackApplication.kt"
      provides: "Notification channel creation at app start"
      contains: "createNotificationChannel"
    - path: "app/src/main/java/hr/fipu/organizationtool/ui/TaskViewModel.kt"
      provides: "sendAchievementNotification called on unlock"
      exports: ["sendAchievementNotification"]
    - path: "app/src/main/java/hr/fipu/organizationtool/MainActivity.kt"
      provides: "Runtime permission request + deep-link intent handling"
      contains: "POST_NOTIFICATIONS"
    - path: "app/src/main/java/hr/fipu/organizationtool/ui/ZenStackApp.kt"
      provides: "ZenStackApp and MainShell accept initialTab to support deep-link"
      contains: "initialTab"
  key_links:
    - from: "TaskViewModel.checkAchievements()"
      to: "NotificationManagerCompat"
      via: "sendAchievementNotification(achievement)"
      pattern: "sendAchievementNotification"
    - from: "PendingIntent in sendAchievementNotification"
      to: "MainActivity"
      via: "Intent with action ACTION_OPEN_ACHIEVEMENTS"
      pattern: "ACTION_OPEN_ACHIEVEMENTS"
    - from: "MainActivity.onCreate / onNewIntent"
      to: "ZenStackApp initialTab parameter"
      via: "intent action check sets openAchievements flag"
      pattern: "MainTab.ACHIEVEMENTS"
---

<objective>
Implement achievement push notifications: request POST_NOTIFICATIONS permission on first launch, fire a system notification when an achievement is unlocked, and deep-link the Achievements tab when the notification is tapped.

Purpose: NOTF-01 — users are informed of achievement unlocks even when the app is not foregrounded.
Output: Permission request flow, notification channel, notification dispatch in ViewModel, deep-link routing through MainActivity into MainShell.
</objective>

<execution_context>
@C:/Users/Kulen/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Kulen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Manifest permission + notification channel in Application</name>
  <files>
    app/src/main/AndroidManifest.xml
    app/src/main/java/hr/fipu/organizationtool/ZenStackApplication.kt
  </files>
  <action>
    In AndroidManifest.xml:
    - Add `<uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>` as a direct child of `<manifest>` (above `<application>`).
    - Add `android:launchMode="singleTop"` to the `<activity android:name=".MainActivity">` element. This prevents a new MainActivity instance being created when the user taps the notification while the app is already open.

    In ZenStackApplication.kt:
    - Add a `createNotificationChannel()` private fun that builds a NotificationChannel with:
      - id: `"achievement_unlocked"`
      - name: `"Achievement Unlocked"`
      - importance: `NotificationManager.IMPORTANCE_DEFAULT`
      - description: `"Notifies when a new achievement is unlocked"`
    - Call `getSystemService(NotificationManager::class.java).createNotificationChannel(channel)` — wrap in `if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O)` (API 26 is below minSdk 28, but the guard is required for compiler satisfaction with the NotificationChannel constructor).
    - Call `createNotificationChannel()` at the end of `onCreate()`, after `startKoin { ... }`.

    Required imports in ZenStackApplication.kt:
    - `android.app.NotificationChannel`
    - `android.app.NotificationManager`
    - `android.os.Build`
  </action>
  <verify>
    Project builds without errors (`./gradlew assembleDebug`). The manifest contains `POST_NOTIFICATIONS`. The Application class contains `createNotificationChannel`.
  </verify>
  <done>
    Manifest declares permission. App creates the "achievement_unlocked" notification channel at startup. launchMode="singleTop" is set on MainActivity.
  </done>
</task>

<task type="auto">
  <name>Task 2: sendAchievementNotification in TaskViewModel + deep-link wiring in MainActivity and ZenStackApp</name>
  <files>
    app/src/main/java/hr/fipu/organizationtool/ui/TaskViewModel.kt
    app/src/main/java/hr/fipu/organizationtool/MainActivity.kt
    app/src/main/java/hr/fipu/organizationtool/ui/ZenStackApp.kt
  </files>
  <action>
    --- TaskViewModel.kt ---

    Add a `sendAchievementNotification(achievement: Achievement)` private fun inside `TaskViewModel`. The ViewModel extends `AndroidViewModel` so `application` context is available.

    Implementation:
    ```
    private fun sendAchievementNotification(achievement: Achievement) {
        val context = getApplication<Application>()
        val intent = Intent(context, MainActivity::class.java).apply {
            action = MainActivity.ACTION_OPEN_ACHIEVEMENTS
            flags = Intent.FLAG_ACTIVITY_SINGLE_TOP or Intent.FLAG_ACTIVITY_CLEAR_TOP
        }
        val pendingIntent = PendingIntent.getActivity(
            context,
            0,
            intent,
            PendingIntent.FLAG_IMMUTABLE or PendingIntent.FLAG_UPDATE_CURRENT
        )
        val notification = NotificationCompat.Builder(context, "achievement_unlocked")
            .setSmallIcon(android.R.drawable.star_on)
            .setContentTitle("Achievement Unlocked!")
            .setContentText(achievement.title)
            .setStyle(NotificationCompat.BigTextStyle().bigText("${achievement.title}: ${achievement.description}"))
            .setAutoCancel(true)
            .setContentIntent(pendingIntent)
            .build()

        if (Build.VERSION.SDK_INT < Build.VERSION_CODES.TIRAMISU ||
            ActivityCompat.checkSelfPermission(context, Manifest.permission.POST_NOTIFICATIONS)
                == PackageManager.PERMISSION_GRANTED) {
            NotificationManagerCompat.from(context).notify(achievement.id.hashCode(), notification)
        }
    }
    ```

    Call `sendAchievementNotification(firstNew)` inside `checkAchievements()`, immediately after `_newlyUnlockedAchievement.value = firstNew` (line ~245). Only call it when `firstNew != null`.

    Required imports:
    - `android.Manifest`
    - `android.app.Application`
    - `android.app.PendingIntent`
    - `android.content.Intent`
    - `android.content.pm.PackageManager`
    - `android.os.Build`
    - `androidx.core.app.ActivityCompat`
    - `androidx.core.app.NotificationCompat`
    - `androidx.core.app.NotificationManagerCompat`

    --- MainActivity.kt ---

    Add a new companion object constant:
    ```
    const val ACTION_OPEN_ACHIEVEMENTS = "hr.fipu.organizationtool.ACTION_OPEN_ACHIEVEMENTS"
    ```
    (Keep the existing `ACTION_VIEW_TASKS` constant.)

    Add a `MutableState<Boolean>` property to hold the deep-link signal:
    ```
    private val _openAchievementsTab = mutableStateOf(false)
    ```

    In `onCreate`, after the existing `ACTION_VIEW_TASKS` check, add:
    ```
    val openAchievements = intent?.action == ACTION_OPEN_ACHIEVEMENTS
    _openAchievementsTab.value = openAchievements
    ```

    Override `onNewIntent` (required because launchMode="singleTop" delivers new intents here when the app is already running):
    ```
    override fun onNewIntent(intent: Intent) {
        super.onNewIntent(intent)
        setIntent(intent)
        _openAchievementsTab.value = intent.action == ACTION_OPEN_ACHIEVEMENTS
    }
    ```

    Update `setContent` to pass `openAchievementsTab`:
    ```
    setContent {
        ZenTheme {
            ZenStackApp(openAchievementsTab = _openAchievementsTab.value)
        }
    }
    ```

    Add `registerForActivityResult` permission launcher for POST_NOTIFICATIONS inside `onCreate` (before `setContent`):
    ```
    if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.TIRAMISU) {
        val requestPermissionLauncher = registerForActivityResult(
            ActivityResultContracts.RequestPermission()
        ) { /* result handled silently — user can deny */ }

        if (ActivityCompat.checkSelfPermission(this, Manifest.permission.POST_NOTIFICATIONS)
                != PackageManager.PERMISSION_GRANTED) {
            requestPermissionLauncher.launch(Manifest.permission.POST_NOTIFICATIONS)
        }
    }
    ```

    Required imports:
    - `android.Manifest`
    - `android.content.Intent`
    - `android.content.pm.PackageManager`
    - `android.os.Build`
    - `androidx.activity.result.contract.ActivityResultContracts`
    - `androidx.compose.runtime.mutableStateOf`
    - `androidx.core.app.ActivityCompat`

    --- ZenStackApp.kt ---

    Update `ZenStackApp` signature to accept `openAchievementsTab: Boolean = false`:
    ```
    @Composable
    fun ZenStackApp(
        viewModel: TaskViewModel = koinViewModel(),
        openAchievementsTab: Boolean = false
    )
    ```

    Pass it down to `MainShell` as `initialTab`:
    ```
    MainShell(
        ...existing params...,
        initialTab = if (openAchievementsTab) MainTab.ACHIEVEMENTS else MainTab.TODAY
    )
    ```

    Update `MainShell` signature to accept `initialTab: MainTab = MainTab.TODAY`:
    ```
    fun MainShell(
        ...,
        initialTab: MainTab = MainTab.TODAY
    )
    ```

    Change the `selectedTab` state initialization to use `initialTab`:
    ```
    var selectedTab by remember { mutableStateOf(initialTab) }
    ```

    Note: `remember` without a key means `initialTab` is only used on first composition. This is correct — the tab state is owned by the composable after first render, and a new navigation intent will recompose through `_openAchievementsTab` state in MainActivity.
  </action>
  <verify>
    `./gradlew assembleDebug` completes without errors or warnings. On a device/emulator running API 33+: install the app, grant the notification permission prompt that appears on first launch. Trigger an achievement unlock (e.g., complete a task to get "first_session") and confirm a notification appears in the shade. Tap the notification and confirm the Achievements tab is selected.
  </verify>
  <done>
    - POST_NOTIFICATIONS dialog appears on first launch on API 33+ devices.
    - Unlocking an achievement produces a system notification showing "Achievement Unlocked!" with the achievement title.
    - Tapping the notification opens MainActivity with the Achievements tab selected.
    - Tapping the notification while the app is foregrounded switches to the Achievements tab without creating a duplicate activity.
  </done>
</task>

</tasks>

<verification>
1. `./gradlew assembleDebug` passes with no errors.
2. AndroidManifest.xml contains `POST_NOTIFICATIONS` permission and `singleTop` on MainActivity.
3. ZenStackApplication.kt creates the `"achievement_unlocked"` channel in `onCreate`.
4. TaskViewModel.kt calls `sendAchievementNotification(firstNew)` when an achievement unlocks.
5. MainActivity.kt requests `POST_NOTIFICATIONS` on API 33+, handles `ACTION_OPEN_ACHIEVEMENTS` in both `onCreate` and `onNewIntent`.
6. ZenStackApp and MainShell accept and use `initialTab` / `openAchievementsTab` parameters.
</verification>

<success_criteria>
1. Users on Android 13+ (API 33) are prompted for notification permission on first app launch.
2. An achievement unlock triggers a notification in the system shade with "Achievement Unlocked!" title and achievement name.
3. Tapping the notification navigates to the Achievements tab inside the app.
4. All three success criteria from the phase goal are satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/16-push-notifications/16-push-notifications-01-SUMMARY.md` using the summary template.
</output>

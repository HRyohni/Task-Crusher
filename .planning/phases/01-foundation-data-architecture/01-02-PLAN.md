---
phase: 01-foundation-data-architecture
plan: "01-02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [app/src/main/java/hr/fipu/organizationtool/ui/theme/Theme.kt, app/src/main/java/hr/fipu/organizationtool/ui/theme/Color.kt, app/src/main/java/hr/fipu/organizationtool/ui/theme/Type.kt, app/src/main/java/hr/fipu/organizationtool/ui/theme/Physics.kt]
autonomous: true
requirements: [CORE-01]

must_haves:
  truths:
    - "The app uses a grayscale + soft indigo/accent palette (per D-01)"
    - "Animations use Material 3 Expressive motion scheme (per research)"
    - "Typography is set to Inter or Roboto sans-serif (per D-03)"
  artifacts:
    - path: "app/src/main/java/hr/fipu/organizationtool/ui/theme/Physics.kt"
      provides: "Global Spring constants (Stiffness/Damping)"
    - path: "app/src/main/java/hr/fipu/organizationtool/ui/theme/Theme.kt"
      provides: "ZenTheme using MotionScheme.expressive()"
  key_links:
    - from: "app/src/main/java/hr/fipu/organizationtool/ui/theme/Theme.kt"
      to: "MotionScheme.expressive()"
      via: "MaterialTheme call"
---

<objective>
Implement the "Zen" Design System using Material 3 Expressive.
Purpose: Establish the tactile feel and minimalist visual identity (Palette, Physics, Typography).
Output: A custom ZenTheme ready for application-wide use.
</objective>

<execution_context>
@M:/programs/organizationTool/.gemini/get-shit-done/workflows/execute-plan.md
@M:/programs/organizationTool/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-data-architecture/01-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Zen Palette and Physics</name>
  <files>app/src/main/java/hr/fipu/organizationtool/ui/theme/Color.kt, app/src/main/java/hr/fipu/organizationtool/ui/theme/Physics.kt</files>
  <action>
    1. Define `ZenPalette` in `Color.kt`: Pure whites, soft grays (surface, background), and a Slate/Indigo accent.
    2. Create `Physics.kt`: 
       - Define `ZenStiffness = Spring.StiffnessMediumLow`.
       - Define `ZenDamping = 0.75f`.
       - Create a helper `zenSpring<T>()` providing these constants.
  </action>
  <verify>
    <automated>grep -E "ZenStiffness|ZenDamping|ZenPalette" app/src/main/java/hr/fipu/organizationtool/ui/theme/*.kt</automated>
  </verify>
  <done>Color palette and motion constants defined per decisions D-01 and D-04</done>
</task>

<task type="auto">
  <name>Task 2: Implement ZenTheme with Expressive Motion</name>
  <files>app/src/main/java/hr/fipu/organizationtool/ui/theme/Theme.kt, app/src/main/java/hr/fipu/organizationtool/ui/theme/Type.kt</files>
  <action>
    1. Update `Type.kt`: Set primary typography to a modern Sans-Serif (Inter/Roboto) per D-03.
    2. In `Theme.kt`:
       - Construct `ZenTheme` using `MaterialTheme`.
       - Set `motionScheme = MotionScheme.expressive()` as researched.
       - Apply the Zen grayscale palette.
  </action>
  <verify>
    <automated>./gradlew assembleDebug</automated>
  </verify>
  <done>ZenTheme implemented with M3 Expressive motion scheme</done>
</task>

<task type="auto">
  <name>Task 3: Implement Physical Shadows Utility</name>
  <files>app/src/main/java/hr/fipu/organizationtool/ui/theme/Shadows.kt</files>
  <action>
    Create a `Modifier.zenShadow()` that uses physical shadows (Subtle offset, soft blur) instead of standard M3 tonal elevation to emphasize tactility (D-02).
  </action>
  <verify>
    <automated>grep "zenShadow" app/src/main/java/hr/fipu/organizationtool/ui/theme/Shadows.kt</automated>
  </verify>
  <done>Physical shadows utility implemented per D-02</done>
</task>

</tasks>

<verification>
Theme compiles and `MotionScheme.expressive()` is utilized in the `MaterialTheme` call.
</verification>

<success_criteria>
The ZenTheme is established, including color, typography, motion physics, and tactile shadow utilities.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-architecture/01-02-SUMMARY.md`
</output>

---
phase: 01-foundation-data-architecture
plan: "01-03"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [app/src/main/java/hr/fipu/organizationtool/data/AppDatabase.kt, app/src/main/java/hr/fipu/organizationtool/data/Task.kt, app/src/main/java/hr/fipu/organizationtool/data/TaskRepository.kt, app/src/main/java/hr/fipu/organizationtool/di/KoinModule.kt]
autonomous: true
requirements: [CORE-02]

must_haves:
  truths:
    - "Room database uses `enableMultiInstanceInvalidation()` to support widget sync (per research)"
    - "Task repository is provided as a single source of truth via Koin 4.0"
    - "Task model includes CORE-02 session state (createdAt, updatedAt, status)"
  artifacts:
    - path: "app/src/main/java/hr/fipu/organizationtool/data/AppDatabase.kt"
      provides: "Room database with multi-process invalidation"
    - path: "app/src/main/java/hr/fipu/organizationtool/di/KoinModule.kt"
      provides: "Koin 4.0 dependency injection"
    - path: "app/src/main/java/hr/fipu/organizationtool/data/Task.kt"
      provides: "Task entity with session tracking fields"
  key_links:
    - from: "AppDatabase.Builder"
      to: "enableMultiInstanceInvalidation()"
      via: "Room setup"
---

<objective>
Implement the Room KMP Data Layer as a Single Source of Truth.
Purpose: Ensure reliable cross-process data synchronization between the App and Glance Widget.
Output: Room DB with Multi-Instance Invalidation and a Repository provided via Koin.
</objective>

<execution_context>
@M:/programs/organizationTool/.gemini/get-shit-done/workflows/execute-plan.md
@M:/programs/organizationTool/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@app/src/main/java/hr/fipu/organizationtool/data/AppDatabase.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Room Multi-Instance Invalidation</name>
  <files>app/src/main/java/hr/fipu/organizationtool/data/AppDatabase.kt</files>
  <action>
    Update `AppDatabase.kt` to include `enableMultiInstanceInvalidation()` in the `Room.databaseBuilder()` call.
    This is critical for sync between the main app process and the Glance widget process (as highlighted in Pitfall 3).
    Ensure the DB is accessible for both app and future Glance processes.
  </action>
  <verify>
    <automated>./gradlew assembleDebug</automated>
  </verify>
  <done>Room DB updated with Multi-Instance Invalidation</done>
</task>

<task type="auto">
  <name>Task 2: Implement Task Repository with Session State</name>
  <files>app/src/main/java/hr/fipu/organizationtool/data/Task.kt, app/src/main/java/hr/fipu/organizationtool/data/TaskRepository.kt, app/src/main/java/hr/fipu/organizationtool/di/KoinModule.kt</files>
  <action>
    1. Create `Task.kt` Entity:
       - Include CORE-02 required fields: `id`, `title`, `priority`.
       - Explicitly add "session state" fields: `createdAt`, `updatedAt`, and `status`.
    2. Create `TaskRepository.kt`:
       - Expose task list and priorities as `Flow`.
       - Handle CRUD operations via `TaskDao`.
       - Ensure state fields are managed during updates.
    3. Create `KoinModule.kt`:
       - Define Koin module for `AppDatabase`, `TaskDao`, and `TaskRepository`.
       - Use `single` for repository instance.
    4. Register Koin in the Application class (create or update `ZenStackApp.kt`).
  </action>
  <verify>
    <automated>grep -E "createdAt|updatedAt|status" app/src/main/java/hr/fipu/organizationtool/data/Task.kt</automated>
  </verify>
  <done>TaskRepository and Koin DI configured with session state support (CORE-02)</done>
</task>

</tasks>

<verification>
App compiles and `TaskRepository` is provided via Koin. Room `enableMultiInstanceInvalidation()` is present.
</verification>

<success_criteria>
The data layer is ready to serve both the app and the future widget, with reliable cross-process sync and session tracking.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-architecture/01-03-SUMMARY.md`
</output>

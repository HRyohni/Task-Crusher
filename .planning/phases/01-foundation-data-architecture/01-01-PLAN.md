---
phase: 01-foundation-data-architecture
plan: "01-01"
type: execute
wave: 1
depends_on: []
files_modified: [gradle/libs.versions.toml, build.gradle.kts, app/build.gradle.kts]
autonomous: true
requirements: [CORE-01]

must_haves:
  truths:
    - "Project compiles with Kotlin 2.1.0 and KSP 2.1.0-1.0.29"
    - "Compose compiler plugin is enabled via the new Jetpack Kotlin plugin"
    - "Koin 4.0.0 is integrated and ready for DI"
  artifacts:
    - path: "gradle/libs.versions.toml"
      provides: "Unified version management for K2 and M3 Expressive"
    - path: "app/build.gradle.kts"
      provides: "KSP and Room compiler configuration"
  key_links:
    - from: "app/build.gradle.kts"
      to: "gradle/libs.versions.toml"
      via: "version catalog aliases"
---

<objective>
Establish the modern Kotlin 2.1 (K2) and Material 3 Expressive infrastructure.
Purpose: Enable the latest performance and motion features required for the ZenStack experience.
Output: Upgraded build configuration and dependency catalog.
</objective>

<execution_context>
@M:/programs/organizationTool/.gemini/get-shit-done/workflows/execute-plan.md
@M:/programs/organizationTool/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Version Catalog</name>
  <files>gradle/libs.versions.toml</files>
  <action>
    Update the following versions in libs.versions.toml:
    - kotlin = "2.1.0"
    - ksp = "2.1.0-1.0.29"
    - material3 = "1.4.0"
    - composeBom = "2025.05.00"
    - room = "2.7.0"
    - koin = "4.0.0"
    - glance = "1.2.0"
    
    Add the new Compose Compiler plugin:
    [plugins]
    compose-compiler = { id = "org.jetbrains.kotlin.plugin.compose", version.ref = "kotlin" }
    
    Ensure material-icons-core and extended are explicitly added (per Pitfall 1).
  </action>
  <verify>
    <automated>./gradlew help</automated>
  </verify>
  <done>Version catalog contains correct 2026-era versions and plugins</done>
</task>

<task type="auto">
  <name>Task 2: Configure Gradle Plugins and Dependencies</name>
  <files>build.gradle.kts, app/build.gradle.kts</files>
  <action>
    1. In root build.gradle.kts: Apply `compose-compiler` plugin and `ksp` plugin.
    2. In app/build.gradle.kts: 
       - Remove old `kotlinCompilerExtensionVersion` if present.
       - Apply `androidx.room` and `org.jetbrains.kotlin.plugin.compose`.
       - Update dependencies to use Koin 4.0 and Material 3 1.4.0.
       - Configure KSP for Room.
  </action>
  <verify>
    <automated>./gradlew assembleDebug</automated>
  </verify>
  <done>Project builds successfully with Kotlin 2.1 and Compose Compiler plugin</done>
</task>

</tasks>

<verification>
Check `assembleDebug` output for "Kotlin 2.1.0" and verify no compiler warnings regarding mismatched Compose versions.
</verification>

<success_criteria>
The project builds and the Android app is ready to use Material 3 Expressive and Room KMP.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-architecture/01-01-SUMMARY.md`
</output>

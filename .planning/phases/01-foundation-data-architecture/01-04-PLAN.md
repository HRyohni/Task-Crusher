---
phase: 01-foundation-data-architecture
plan: "01-04"
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified: [app/src/main/java/hr/fipu/organizationtool/ui/FoundationLab.kt, app/src/main/java/hr/fipu/organizationtool/MainActivity.kt]
autonomous: false
requirements: [CORE-01, CORE-02]

must_haves:
  truths:
    - "ZenTheme grayscale palette is correctly applied to UI components"
    - "Task repository is successfully used to load/save a test task"
    - "Physical shadow (zenShadow) is visually distinct from tonal elevation"
  artifacts:
    - path: "app/src/main/java/hr/fipu/organizationtool/ui/FoundationLab.kt"
      provides: "Verification UI screen"
  key_links:
    - from: "FoundationLab"
      to: "ZenTheme"
      via: "Compose rendering"
    - from: "FoundationLab"
      to: "TaskRepository"
      via: "Koin injection"
---

<objective>
Verify the integration of the Foundation and Data Layer.
Purpose: Ensure all Wave 1 & 2 components work together (Theme + DB + DI).
Output: A functional "Foundation Lab" screen and automated DI verification.
</objective>

<execution_context>
@M:/programs/organizationTool/.gemini/get-shit-done/workflows/execute-plan.md
@M:/programs/organizationTool/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-data-architecture/01-02-SUMMARY.md
@.planning/phases/01-foundation-data-architecture/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Foundation Lab Verification UI</name>
  <files>app/src/main/java/hr/fipu/organizationtool/ui/FoundationLab.kt, app/src/main/java/hr/fipu/organizationtool/MainActivity.kt</files>
  <action>
    1. Create `FoundationLab.kt`:
       - A minimalist Compose screen using `ZenTheme`.
       - Inject `TaskRepository` via Koin.
       - Display a test task from Room.
       - Include a button that applies `Modifier.zenShadow()` to a card.
    2. Set `MainActivity.kt` to launch `FoundationLab`.
  </action>
  <verify>
    <automated>./gradlew assembleDebug</automated>
  </verify>
  <done>Verification screen implemented using all Phase 1 foundations</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Foundation Lab UI (Theme + DB + DI)</what-built>
  <how-to-verify>
    1. Run the app: `./gradlew installDebug`
    2. Observe the "Zen" grayscale palette and accent color.
    3. Verify that a test task is loaded/displayed.
    4. Confirm the "tactile" feel of the shadow on the test card.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
App runs, displays Zen-themed UI, and successfully retrieves data from Room via Koin-injected repository.
</verification>

<success_criteria>
Integration of Kotlin 2.1, M3 Expressive theme, and Room KMP repository is confirmed.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-architecture/01-04-SUMMARY.md`
</output>

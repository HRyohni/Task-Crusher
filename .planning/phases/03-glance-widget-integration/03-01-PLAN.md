---
phase: 03-glance-widget-integration
plan: "03-01"
type: execute
wave: 1
depends_on: []
files_modified: [gradle/libs.versions.toml, app/src/main/java/hr/fipu/organizationtool/widget/ZenStackWidget.kt]
autonomous: true
requirements: [WIDG-01]

must_haves:
  truths:
    - "Widget uses ZenIndigo for primary accents and ZenSurface for backgrounds"
    - "LinearProgressIndicator displays completion ratio of Priority tasks"
    - "Glance version is upgraded to 1.2.0"
  artifacts:
    - path: "app/src/main/java/hr/fipu/organizationtool/widget/ZenStackWidget.kt"
      provides: "Reactive UI with LinearProgressIndicator"
  key_links:
    - from: "ZenStackWidget.kt"
      to: "TaskDao"
      via: "collectAsState"
---

<objective>
Refactor the ZenStack widget UI to follow the Zen design system and implement a visual progress tracker.
Purpose: Provide users with a glanceable, high-fidelity status of their "Power 3" tasks.
Output: Updated ZenStackWidget with reactive progress bar and Zen palette.
</objective>

<execution_context>
@M:/programs/organizationTool/.gemini/get-shit-done/workflows/execute-plan.md
@M:/programs/organizationTool/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-glance-widget-integration/03-CONTEXT.md
@app/src/main/java/hr/fipu/organizationtool/ui/theme/Color.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade Glance and Define Zen Colors for Widget</name>
  <files>gradle/libs.versions.toml, app/src/main/java/hr/fipu/organizationtool/widget/ZenStackWidget.kt</files>
  <action>
    1. Update `libs.versions.toml`: Set `glance = "1.2.0"`.
    2. In `ZenStackWidget.kt`, define a `GlanceTheme` (or manual ColorProvider) using:
       - ZenIndigo: #5E5CE6 (Primary)
       - ZenSurface: #F5F5F7 (Background)
       - ZenGrayDark: #1C1C1E (Text)
    3. Replace `android.R.drawable.dialog_holo_light_frame` with a `GlanceModifier.background(ColorProvider(ZenSurface))` and `GlanceModifier.cornerRadius(12.dp)`.
  </action>
  <verify>
    <automated>./gradlew help</automated>
  </verify>
  <done>Glance upgraded and Zen palette applied to widget root</done>
</task>

<task type="auto">
  <name>Task 2: Implement Reactive LinearProgressIndicator</name>
  <files>app/src/main/java/hr/fipu/organizationtool/widget/ZenStackWidget.kt</files>
  <action>
    1. Import `androidx.glance.appwidget.LinearProgressIndicator`.
    2. Calculate `progress = completedCount.toFloat() / priorityTasks.size.coerceAtLeast(1)`.
    3. Insert `LinearProgressIndicator` below the header text.
    4. Set its progress and color (ZenIndigo).
    5. Ensure the text "Done: X/Y" remains for clarity.
  </action>
  <verify>
    <automated>grep -q "LinearProgressIndicator" app/src/main/java/hr/fipu/organizationtool/widget/ZenStackWidget.kt</automated>
  </verify>
  <done>Visual progress bar correctly reflects priority task completion</done>
</task>

</tasks>

<verification>
1. Deploy to emulator.
2. Add ZenStack widget.
3. Verify visual appearance matches the "Flat Zen Palette".
4. Check that the progress bar fills as tasks are completed in the app.
</verification>

<success_criteria>
The widget displays a functional LinearProgressIndicator using Zen colors, and it updates reactively when the DB changes.
</success_criteria>

<output>
After completion, create `.planning/phases/03-glance-widget-integration/03-01-SUMMARY.md`
</output>

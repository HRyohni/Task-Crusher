---
phase: 03-glance-widget-integration
plan: "03-02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [app/src/main/java/hr/fipu/organizationtool/MainActivity.kt, app/src/main/java/hr/fipu/organizationtool/widget/ZenStackWidget.kt]
autonomous: true
requirements: [Interactivity]

must_haves:
  truths:
    - "Tapping the widget background (non-task area) opens the app"
    - "The app correctly identifies the intent as coming from the widget"
  artifacts:
    - path: "app/src/main/java/hr/fipu/organizationtool/MainActivity.kt"
      provides: "Deep link handling logic"
  key_links:
    - from: "ZenStackWidget.kt"
      to: "MainActivity"
      via: "actionStartActivity"
---

<objective>
Enable deep linking from the widget background to the app's core tasks view.
Purpose: Provide a frictionless transition from viewing status to managing tasks.
Output: Integrated background tap action on the widget and intent handling in MainActivity.
</objective>

<execution_context>
@M:/programs/organizationTool/.gemini/get-shit-done/workflows/execute-plan.md
@M:/programs/organizationTool/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-glance-widget-integration/03-CONTEXT.md
@app/src/main/java/hr/fipu/organizationtool/widget/ZenStackWidget.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Deep Link Support to MainActivity</name>
  <files>app/src/main/java/hr/fipu/organizationtool/MainActivity.kt</files>
  <action>
    1. Define a constant `ACTION_VIEW_TASKS = "hr.fipu.organizationtool.ACTION_VIEW_TASKS"` in `MainActivity` or a separate companion object.
    2. In `onCreate`, check the intent: `intent.action == ACTION_VIEW_TASKS`.
    3. For now, log the detection of the deep link and ensure `FoundationLab` is shown. 
    4. Note: If a navigation system is later added, this will serve as the entry point for specific routing.
  </action>
  <verify>
    <automated>grep -q "ACTION_VIEW_TASKS" app/src/main/java/hr/fipu/organizationtool/MainActivity.kt</automated>
  </verify>
  <done>MainActivity is prepared to receive and handle deep links from the widget</done>
</task>

<task type="auto">
  <name>Task 2: Wire Widget Background for Action</name>
  <files>app/src/main/java/hr/fipu/organizationtool/widget/ZenStackWidget.kt</files>
  <action>
    1. Wrap the root `Column` in `ZenStackWidget` with a `Box` or apply `clickable` to the `Column`.
    2. Use `actionStartActivity<MainActivity>(action = MainActivity.ACTION_VIEW_TASKS)` (per D-03).
    3. Ensure individual `TaskItem` clickability (toggling tasks) still works and doesn't trigger the background action.
  </action>
  <verify>
    <automated>grep -q "actionStartActivity" app/src/main/java/hr/fipu/organizationtool/widget/ZenStackWidget.kt</automated>
  </verify>
  <done>Widget background correctly triggers the MainActivity with the deep link intent</done>
</task>

</tasks>

<verification>
1. Launch app from widget by tapping on any white/background area.
2. Verify app opens immediately.
3. Check Logcat for "Deep link detected" message (if added in Task 1).
</verification>

<success_criteria>
The widget's background area is clickable and launches the app via a specific intent action.
</success_criteria>

<output>
After completion, create `.planning/phases/03-glance-widget-integration/03-02-SUMMARY.md`
</output>

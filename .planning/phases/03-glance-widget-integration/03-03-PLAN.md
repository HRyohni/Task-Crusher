---
phase: 03-glance-widget-integration
plan: "03-03"
type: execute
wave: 3
depends_on: ["03-02"]
files_modified: [app/src/main/java/hr/fipu/organizationtool/widget/TaskActionHandler.kt, app/src/main/res/raw/zen_success.mp3]
autonomous: true
requirements: [WIDG-02]

must_haves:
  truths:
    - "Checking a task via widget triggers a custom Zen sound"
    - "Success sound does not crash if file is missing (error handling)"
  artifacts:
    - path: "app/src/main/res/raw/zen_success.mp3"
      provides: "Custom sensory feedback asset"
  key_links:
    - from: "ToggleTaskCallback"
      to: "MediaPlayer"
      via: "R.raw.zen_success"
---

<objective>
Implement sensory feedback for widget interactions and ensure robust data synchronization.
Purpose: Create a satisfying "Zen" moment upon task completion and maintain data integrity.
Output: Custom sound integration and verified Room synchronization.
</objective>

<execution_context>
@M:/programs/organizationTool/.gemini/get-shit-done/workflows/execute-plan.md
@M:/programs/organizationTool/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-glance-widget-integration/03-CONTEXT.md
@.planning/phases/03-glance-widget-integration/03-RESEARCH.md
@app/src/main/java/hr/fipu/organizationtool/widget/TaskActionHandler.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Custom Zen Sound Asset</name>
  <files>app/src/main/res/raw/zen_success.mp3</files>
  <action>
    1. Create the `app/src/main/res/raw/` directory.
    2. Note: I cannot "create" a real .mp3 file binary content easily with text tools. 
    3. Action: Create a 0-byte file named `zen_success.mp3` as a placeholder.
    4. Provide instructions to the user to replace it with a real short "ding" sound.
  </action>
  <verify>
    <automated>Test-Path app/src/main/res/raw/zen_success.mp3</automated>
  </verify>
  <done>Resource directory created and placeholder asset placed</done>
</task>

<task type="auto">
  <name>Task 2: Update ActionHandler with Custom Sound</name>
  <files>app/src/main/java/hr/fipu/organizationtool/widget/TaskActionHandler.kt</files>
  <action>
    1. Update `ToggleTaskCallback.playSuccessSound` to use `MediaPlayer.create(context, R.raw.zen_success)`.
    2. Ensure `mp.start()` is called and `mp.setOnCompletionListener { it.release() }` is used for cleanup.
    3. Add a try-catch block to prevent crashes if the resource is invalid or missing.
  </action>
  <verify>
    <automated>grep -q "R.raw.zen_success" app/src/main/java/hr/fipu/organizationtool/widget/TaskActionHandler.kt</automated>
  </verify>
  <done>Widget interactions now trigger the custom Zen success sound</done>
</task>

</tasks>

<verification>
1. Run app and add widget.
2. Toggle a task on the widget.
3. Verify that the app doesn't crash and (if placeholder replaced) the sound plays.
</verification>

<success_criteria>
The widget provides tactile (checkbox update) and auditory (custom ding) feedback when completing tasks.
</success_criteria>

<output>
After completion, create `.planning/phases/03-glance-widget-integration/03-03-SUMMARY.md`
</output>

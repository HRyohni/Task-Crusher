---
phase: 13-widget-overhaul
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/hr/fipu/organizationtool/widget/ZenStackWidget.kt
autonomous: true

must_haves:
  truths:
    - "Priority section is labeled 'Priority' in bold"
    - "Brain Dump section is labeled 'Brain Dump' in lighter style (only visible when active tasks exist)"
    - "Priority task rows have a visible background highlight distinct from brain dump rows"
    - "Priority task rows use larger font (18sp checkbox, 14sp text) compared to brain dump rows (14sp checkbox, 12sp text)"
  artifacts:
    - path: "app/src/main/java/hr/fipu/organizationtool/widget/ZenStackWidget.kt"
      provides: "Widget with highlighted priority rows and confirmed section labels"
      contains: "background(ColorProvider"
  key_links:
    - from: "TaskItem(task)"
      to: "GlanceModifier.background"
      via: "isSmall=false branch applies ZenIndigo tint background"
      pattern: "background.*ColorProvider"
---

<objective>
Polish WIDG-06 section labels (verify and tighten styling) and implement WIDG-07 priority row highlight background.

Purpose: Visually distinguish priority tasks from brain dump tasks so the widget communicates hierarchy at a glance.
Output: ZenStackWidget.kt with priority rows carrying a subtle ZenIndigo tint background, and confirmed readable section labels.
</objective>

<execution_context>
@C:/Users/Kulen/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Kulen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/src/main/java/hr/fipu/organizationtool/widget/ZenStackWidget.kt
@app/src/main/java/hr/fipu/organizationtool/ui/theme/Color.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add priority row highlight background (WIDG-07)</name>
  <files>app/src/main/java/hr/fipu/organizationtool/widget/ZenStackWidget.kt</files>
  <action>
In the `TaskItem` composable, add a `background` modifier to the Row when `isSmall == false` (i.e., priority rows). Also add `cornerRadius(8.dp)` and horizontal padding to make the highlight pill look clean.

Replace the existing Row modifier chain (lines 107-112) with:

```kotlin
modifier = GlanceModifier
    .fillMaxWidth()
    .padding(vertical = 3.dp)
    .let {
        if (!isSmall) it.background(ColorProvider(ZenIndigo.copy(alpha = 0.08f)))
            .cornerRadius(8.dp)
            .padding(horizontal = 6.dp, vertical = 2.dp)
        else it.padding(horizontal = 2.dp)
    }
    .clickable(actionRunCallback<ToggleTaskCallback>(
        parameters = actionParametersOf(TaskActionHandler.TaskIdKey to task.id)
    )),
```

Note: Glance `GlanceModifier` does not support `let` chaining — use a `val modifier` local variable with an if/else assignment instead:

```kotlin
val rowModifier = if (!isSmall) {
    GlanceModifier
        .fillMaxWidth()
        .padding(vertical = 3.dp)
        .background(ColorProvider(ZenIndigo.copy(alpha = 0.08f)))
        .cornerRadius(8.dp)
        .padding(horizontal = 6.dp, vertical = 2.dp)
        .clickable(actionRunCallback<ToggleTaskCallback>(
            parameters = actionParametersOf(TaskActionHandler.TaskIdKey to task.id)
        ))
} else {
    GlanceModifier
        .fillMaxWidth()
        .padding(vertical = 3.dp, horizontal = 2.dp)
        .clickable(actionRunCallback<ToggleTaskCallback>(
            parameters = actionParametersOf(TaskActionHandler.TaskIdKey to task.id)
        ))
}
Row(
    modifier = rowModifier,
    verticalAlignment = Alignment.CenterVertically
) {
```

The `cornerRadius` import is already present (`androidx.glance.appwidget.cornerRadius`). No new imports needed beyond what exists.

WIDG-06 note: Both "Priority" and "Brain Dump" section labels already exist in the LazyColumn items. No structural change needed — just verify they are present and styled correctly. The "Priority" label uses `fontWeight = FontWeight.Bold, fontSize = 13.sp` and "Brain Dump" uses `fontSize = 11.sp, alpha = 0.6f`. These are correct — leave them unchanged.
  </action>
  <verify>Build the project: `./gradlew :app:assembleDebug`. Build must succeed with zero errors. Install on device/emulator and confirm priority task rows show a faint indigo tint background with rounded corners; brain dump rows have no background tint.</verify>
  <done>Priority task rows render with subtle ZenIndigo tint background. Brain dump rows remain plain. Both section labels ("Priority" and "Brain Dump") are visible with their existing styles. Build passes with no errors.</done>
</task>

</tasks>

<verification>
Run `./gradlew :app:assembleDebug` — zero errors.
On device: widget shows priority rows with faint indigo-tinted rounded background, brain dump rows without background, both section labels legible.
</verification>

<success_criteria>
- `./gradlew :app:assembleDebug` succeeds
- Priority rows have background highlight (ZenIndigo at ~8% alpha with cornerRadius)
- Brain dump rows have no background tint
- "Priority" label bold 13sp and "Brain Dump" label 11sp 60% alpha both present
</success_criteria>

<output>
After completion, create `.planning/phases/13-widget-overhaul/13-01-SUMMARY.md`
</output>
